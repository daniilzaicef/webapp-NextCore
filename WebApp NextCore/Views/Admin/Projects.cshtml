@model IEnumerable<ProjectModel>

<link href="~/css/Admin.css" rel="stylesheet" />

<div class="table-wrapper">

    <h2>Проекты</h2>

    <a asp-action="CreateProject" class="btn btn-success mb-3">Добавить проект</a>

    <table class="custom-table">
        <thead class="table-dark">
            <tr>
                <th>Название</th>
                <th>Технологии</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="title-cell">@item.Title</td>
                    <td class="desc-cell">@item.Technologies</td>
                    <td>
                        <a asp-action="EditProject" asp-route-id="@item.Id" 
                        class="btn-table edit">
                        Изменить
                        </a>

                        <a asp-action="DeleteProject" asp-route-id="@item.Id"
                           onclick="return confirm('Удалить проект?')"
                           class="btn-table delete">
                           Удалить
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>